<input id="esForm" type="hidden" value=true />
  <div class="row">
   <div class="panel">
    <%= form_tag actualizar_multiples_rco_monedas_path, method: :put do %>
      <% @rco_monedas.each do |rco_moneda| %>

        <div class="row">
        <h3>Empresa: <%= rco_moneda.nombre.upcase %></h3>
          <div class="panel">
            <%= fields_for "rco_monedas[]", rco_moneda, builder: EstandarFormBuilder do |f| %>
            <%= render 'form_fields', f:f %>
            <% end %>
          </div>
        </div>
    <% end %>
    <div class="row">
      <div class="small-12 columns">
      <input id="nodo" name="nodo" type="hidden" value=<%= @nodo %> /> 
        <%= submit_tag "Actualizar", :class => 'button tiny boton_largo' %>
      </div>
    </div>
    <% end %>
  </div>
</div>