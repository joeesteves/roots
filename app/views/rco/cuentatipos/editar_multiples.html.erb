<input id="esForm" type="hidden" value=true />
  <div class="row">
   <div class="panel">
    <%= form_tag actualizar_multiples_rco_cuentatipos_path, method: :put do %>
      <% @rco_cuentatipos.each do |rco_cuentatipo| %>

        <div class="row">
        <h3>Empresa: <%= rco_cuentatipo.nombre.upcase %></h3>
          <div class="panel">
            <%= fields_for "rco_cuentatipos[]", rco_cuentatipo, builder: EstandarFormBuilder do |f| %>
            <%= render 'form_fields', f:f %>
            <% end %>
          </div>
        </div>
    <% end %>
    <div class="row">
      <div class="small-12 columns">
      <input id="nodo" name="nodo" type="hidden" value=<%= @nodo %> /> 
        <%= submit_tag "Actualizar", :class => 'button tiny boton_largo' %>
      </div>
    </div>
    <% end %>
  </div>
</div>