<input id="esForm" type="hidden" value=true />
  <div class="row">
   <div class="panel">
    <%= form_tag actualizar_multiples_rba_libros_path, method: :put do %>
      <% @rba_libros.each do |rba_libro| %>

        <div class="row">
        <h3>Empresa: <%= rba_libro.nombre.upcase %></h3>
          <div class="panel">
            <%= fields_for "rba_libros[]", rba_libro, builder: EstandarFormBuilder do |f| %>
            <%= render 'form_fields', f:f %>
            <% end %>
          </div>
        </div>
    <% end %>
    <div class="row">
      <div class="small-12 columns">
        <%= submit_tag "Actualizar", :class => 'button tiny boton_largo' %>
      </div>
    </div>
    <% end %>
  </div>
</div>