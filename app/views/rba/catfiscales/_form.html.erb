<input id="esForm" type="hidden" value=true />
<div class="row">
  <div class="panel">
  <%= form_for @rba_catfiscal, builder: EstandarFormBuilder do |f| %>
  <%= render 'form_fields', f:f %>
 
    <div class="row">
      <div class="small-12 columns">
        <%= f.submit %>
      </div>
    </div>
  <% end %>
  </div>
</div>
